server:
  port: 8088

logging:
  level:
    org.springframework: debug
spring:
  messages:
    basename: messages

  devtools:
    livereload:
      enabled: true

#  security:
#    user:
#      name: username
#      password: 1234
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
